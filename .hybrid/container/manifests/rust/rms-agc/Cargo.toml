[package]
name = "usbradioplus_agc"
version = "0.1.0-alpha19"
edition = "2024"
rust-version = "1.85"
license = "MIT"
description = "FFmpeg-hosted USBRadioPlus RMS gain rider with the stable LADSPA interface"
publish = false

[lib]
crate-type = ["cdylib"]

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
missing_docs = "deny"

[lints.clippy]
undocumented_unsafe_blocks = "deny"
